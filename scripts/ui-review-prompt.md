Review the attached `trekr` UI screenshots as a compact UX and layout review.

Focus on:
- text overlap
- clipped labels
- inconsistent spacing
- controls that are unclear or misleading
- weak hierarchy or state communication
- anything that makes the page hard to understand quickly

Treat this as a read-only visual QA pass, not a code review.

Output format:
- Start with `Findings`
- Use a flat numbered list
- Each item must include:
  - severity: `high`, `medium`, or `low`
  - screenshot/page name
  - issue
  - brief suggested fix
- If there are no findings, say `No findings.`

Keep the response concise and practical.
