Findings

1. severity: medium; screenshot/page: `midi-io.png`; issue: the large light-gray content regions look like blank/disabled panels without explanation, so it is unclear whether ports are missing, loading, or intentionally hidden; brief suggested fix: add explicit empty/loading state text inside each panel and reduce placeholder fill prominence.
2. severity: medium; screenshot/page: `timeline.png`; issue: top control rows are very dense with many equal-weight toggles (`PLAY`, `RECORD`, `MODE`, `LAUNCHQ`, `PEERS`), making scan order and current state hard to parse quickly; brief suggested fix: group controls into labeled clusters with stronger spacing and emphasize active states with higher-contrast fill.
3. severity: medium; screenshot/page: `routing.png`; issue: `SET` and `TGL` controls repeat with similar visual weight to values, so action buttons and current values are easy to confuse; brief suggested fix: style action buttons more distinctly (button chrome/contrast) and visually separate them from value fields.
4. severity: low; screenshot/page: `mappings.png`; issue: bottom shortcut/help strip is cramped and low-contrast, so discoverability of key actions is weak; brief suggested fix: increase contrast and spacing, and split commands into smaller grouped segments.
5. severity: low; screenshot/page: `mappings-overlay.png`; issue: large unused vertical area under the table weakens hierarchy and makes the overlay feel visually unbalanced; brief suggested fix: tighten overlay height to content or add contextual guidance/filters in the lower area.
6. severity: low; screenshot/page: `timeline-focused.png`; issue: focused-track mode still keeps many tiny, low-legibility status labels in the header, which dilutes the “focused” intent; brief suggested fix: simplify header content in focused mode and enlarge only track-relevant status indicators.