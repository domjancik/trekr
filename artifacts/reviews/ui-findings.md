Findings

1. severity: medium; screenshot/page name: `timeline`; issue: the top control strip is too dense and reads like a wall of equal-weight toggles, so key state like playback/record mode and timing settings is hard to scan quickly; brief suggested fix: group controls into labeled clusters and give primary transport/state controls stronger visual emphasis than secondary toggles.
2. severity: medium; screenshot/page name: `timeline`; issue: track headers are cramped, with labels like `TRACK 1` competing with `ARM/REC/MUTE/SOLO` and tiny meter/status elements, making the per-track state hard to parse; brief suggested fix: increase header height or split name/status from action toggles so each track’s state has clearer hierarchy.
3. severity: low; screenshot/page name: `timeline`; issue: the selected columns use similar thin outlines and muted fills to surrounding columns, so selection state is visible but weak; brief suggested fix: strengthen selected-state contrast with a more distinct fill, border, or header treatment.
4. severity: medium; screenshot/page name: `mappings`; issue: the table rows are extremely compressed, and columns such as `SOURCE`, `TARGET`, `SCOPE`, and `ON` have minimal breathing room, which slows row scanning; brief suggested fix: add more row height and slightly widen or rebalance columns so row content is easier to separate visually.
5. severity: medium; screenshot/page name: `mappings`; issue: the top action chips (`TAP MODE`, `TAP LEARN`, `TAP DIRECT MAP`) look similar to passive status fields, so it is unclear which are clickable controls versus readouts; brief suggested fix: differentiate interactive controls from status indicators through stronger button styling and clearer active/inactive states.
6. severity: low; screenshot/page name: `mappings`; issue: shorthand labels like `ACT TRACK`, `ARMED/ACT`, and footer hints such as `TAP AGAIN ACT` are terse enough to be hard to decode on first read; brief suggested fix: expand labels where space allows or provide clearer wording/abbreviations with more consistent terminology.
7. severity: medium; screenshot/page name: `mappings-overlay`; issue: the overlay has weak hierarchy because the title, help text, table, and pagination/status all use very similar scale and contrast; brief suggested fix: enlarge the title area and separate instructions/status from table content with more spacing and contrast.
8. severity: low; screenshot/page name: `mappings-overlay`; issue: the `ROWS 1-19 / 30` and `SCOPE` labels float at the upper right with ambiguous association to the table below; brief suggested fix: align these with column headers or place them in a dedicated toolbar/status row.
9. severity: medium; screenshot/page name: `midi-io`; issue: the large empty device panels dominate the page, while the actual selected-state indicators (`DEF`, `SEL`) are tiny and easy to miss; brief suggested fix: reduce empty panel weight and make selected/default device states more prominent near each panel heading.
10. severity: low; screenshot/page name: `midi-io`; issue: the stacked output cards use similar framing but different heights without a strong explanation, which makes the layout feel uneven; brief suggested fix: normalize card structure or add clearer section labels for primary versus secondary outputs.
11. severity: medium; screenshot/page name: `routing`; issue: the row controls mix different action labels (`SELECT`, `TOGGLE`, `TAP +/-`) and small plus/minus buttons with inconsistent patterns, making interaction rules hard to infer; brief suggested fix: standardize row-end controls so similar field types use the same affordance pattern.
12. severity: medium; screenshot/page name: `routing`; issue: color is doing most of the work to distinguish routing fields, but the meaning of each color is not obvious and may not help first-time understanding; brief suggested fix: add stronger textual grouping or field-type labels so color supports meaning rather than carrying it alone.
13. severity: low; screenshot/page name: `routing`; issue: the top mode bar (`ACTIVE T1`, `THRU OFF`, `TRACK 1`) lacks clear hierarchy, so it is not obvious which value is the page mode versus current target; brief suggested fix: separate page mode, current target track, and secondary state into distinct labeled regions.
14. severity: low; screenshot/page name: `all pages`; issue: footer shortcuts (`F5 MAPPINGS`, `F7 DISCOVER`, `F8 DIRECT`) are visually understated despite being global navigation/actions; brief suggested fix: either promote them as persistent navigation or reduce them further if they are secondary, so their importance is unambiguous.