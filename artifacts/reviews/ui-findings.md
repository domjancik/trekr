Findings
1. severity: high; screenshot/page: `timeline.png`; issue: the 6-track vertical layout is overly dense, with tiny labels (`THRU TRACK`, `ADD OUTPUT FX`, step numbers) competing for space and slowing scan time; brief suggested fix: increase minimum track column width (or show fewer tracks at once) and move secondary labels into a collapsible/details layer.
2. severity: medium; screenshot/page: `timeline.png`; issue: several footer/control labels in each track card appear cramped and close to borders (for example `VEL VL 120%`, `ORD UP -`), which reads like near-clipping; brief suggested fix: add horizontal padding and enforce truncation rules with ellipsis for long tokens.
3. severity: medium; screenshot/page: `routing.png`; issue: control intent is unclear for repeated tiny buttons (`+`, `SET`, `TGL`) because they look identical across very different actions; brief suggested fix: add short action labels/tooltips or icon variants per function class (edit vs toggle vs commit).
4. severity: medium; screenshot/page: `midi-io.png`; issue: large empty gray panes dominate each device card without clear meaning (content area vs meter vs placeholder), weakening hierarchy; brief suggested fix: label those regions explicitly or reduce their visual weight and surface key device status fields instead.
5. severity: low; screenshot/page: `mappings.png`; issue: the bottom shortcut/help strip is crowded and visually noisy, making key actions hard to parse quickly; brief suggested fix: group shortcuts by category with spacing/dividers and de-emphasize non-primary bindings.
6. severity: low; screenshot/page: `mappings-overlay.png`; issue: overlay state communication is weak because `W WRITE` in the header reads like passive text rather than an actionable mode/state; brief suggested fix: render write mode as a clear state badge/button (`WRITE: OFF/ON`) with stronger contrast.
7. severity: low; screenshot/page: `timeline-focused.png`; issue: top status chips (`LINK OFF`, `START/STOP OFF`, `F6`, `SHIFT+F6`) are visually compressed and hard to decode as state vs shortcut; brief suggested fix: separate state indicators from key-hint chips and add consistent spacing/alignment rules.