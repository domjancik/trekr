Findings

1. severity: high | screenshot/page name: `timeline.png` | issue: Information density is too high (6 narrow track columns, tiny labels, dense control strip), so scanning state quickly is difficult. | brief suggested fix: Default to a grouped/condensed view (fewer visible tracks) and promote key song/transport state into a larger summary bar.
2. severity: high | screenshot/page name: `routing.png` | issue: In `REC/MON`, row labels and values look visually stacked/compressed (hard to parse row boundaries at a glance), which risks misreading monitor/record state. | brief suggested fix: Increase row height and add clearer row separators with a fixed label/value/toggle grid alignment.
3. severity: medium | screenshot/page name: `routing.png` | issue: Control labels like `INPUT UAL` / `OUTPUT UAL` are unclear and read like truncation/typos, which makes intent ambiguous. | brief suggested fix: Rename to explicit labels (`INPUT VALUE`, `OUTPUT VALUE`) and keep naming consistent with `TAP VALUE`.
4. severity: medium | screenshot/page name: `midi-io.png` | issue: Selected/default state is communicated by very small `DEF SEL` tags only; cards otherwise look similarly inactive. | brief suggested fix: Add stronger selected/default treatment (border, fill, and a persistent badge area) plus a clearer “current default” indicator.
5. severity: medium | screenshot/page name: `mappings.png` | issue: Bottom shortcut/help strip is overloaded and low-hierarchy; primary vs secondary actions are hard to distinguish quickly. | brief suggested fix: Group shortcuts into 2-3 labeled clusters (navigation/edit/learn) and visually emphasize only the primary action keys.
6. severity: low | screenshot/page name: `mappings-overlay.png` | issue: Overlay pagination/state is weakly communicated (`ROWS 1-19 / 30` only), so discoverability of additional rows is low. | brief suggested fix: Add explicit paging/scroll affordances (e.g., `PgUp/PgDn`, arrows, or scrollbar marker) near the row counter.